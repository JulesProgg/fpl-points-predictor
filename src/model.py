from typing import List

def predict_points(n: int = 5) -> List[float]:
    """
    Renvoie n prédictions factices pour tester le pipeline/CLI.
    Remplace ensuite par ton vrai modèle.
    """
    return [4.0] * n